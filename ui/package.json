{
  "name": "ui",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil App Starter",
  "scripts": {
    "build": "stencil build --prod",
    "start": "npm outdated && stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil-community/router": "^1.0.2",
    "@stencil/core": "^4.21.0",
    "@stencil/sass": "^3.0.12",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "jest-cli": "^29.7.0",
    "puppeteer": "^23.2.1"
  },
  "license": "MIT",
  "dependencies": {
    "canvas-confetti": "^1.9.3"
  }
}
